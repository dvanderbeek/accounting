<%= form_with(model: onchain_billing_contract) do |form| %>
  <% if onchain_billing_contract.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(onchain_billing_contract.errors.count, "error") %> prohibited this onchain_billing_contract from being saved:</h2>

      <ul>
        <% onchain_billing_contract.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tab, style: "display: block" %>
    <%= form.text_field :tab %>
  </div>

  <div>
    <%= form.label :org_id, style: "display: block" %>
    <%= form.text_field :org_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
