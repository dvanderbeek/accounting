<%= form_with(model: billing_subscription) do |form| %>
  <% if billing_subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(billing_subscription.errors.count, "error") %> prohibited this billing_subscription from being saved:</h2>

      <ul>
        <% billing_subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :org_id, style: "display: block" %>
    <%= form.text_field :org_id %>
  </div>

  <div>
    <%= form.label :price_id, style: "display: block" %>
    <%= form.text_field :price_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
